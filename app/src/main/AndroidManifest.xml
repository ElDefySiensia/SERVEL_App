<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SERVEL_App">

        <!-- Aqui las activities -->
        <activity android:name=".CambioDomicilioActivity" android:screenOrientation="portrait"/> <!-- El portrait es porque soy un desgraciado -->
        <activity android:name=".ModificarPerfilActivity" android:screenOrientation="portrait"/> <!-- Y no queria compatibilizar con landscape -->
        <activity android:name=".VerPerfilActivity" android:screenOrientation="portrait"/> <!-- jejeje -->
        <activity android:name=".CandidatosActivity" android:screenOrientation="portrait"/>
        <activity android:name=".VotoExteriorActivity" android:screenOrientation="portrait"/>
        <activity android:name=".TramitesActivity" android:screenOrientation="portrait"/>
        <activity android:name=".DatosElectoralesActivity" android:screenOrientation="portrait"/>
        <activity android:name=".VotoEnLineaActivity" android:screenOrientation="portrait"/>
        <activity android:name=".PortalActivity" android:screenOrientation="portrait"/>
        <activity android:name=".RegistroActivity" android:screenOrientation="portrait"/>
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>